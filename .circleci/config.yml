version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build-and-test:
    executor:
      name: node/deafult
    steps:
      - checkout
      - node/with-cache:
         steps:
           - run: npm install
           - run: npm test
workflows:
  node-tests:
    jobs:
      - build-and-test
